{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "lint": {
      "inputs": ["**/*.ts", "**/*.tsx", "../../eslint.config.mjs", "../../tools/eslint-rules/**"]
    },
    "test": {
      "dependsOn": ["^build"]
    }
  }
}
